def minStr(str1, str2):
    while True:
        arr = []
        for i in range(len(str2)):
            if str2[i] not in str1:
                return 0
            else:
                arr.append(str2.find(str[i]))
        return str2[]
